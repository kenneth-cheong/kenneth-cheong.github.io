# Copy to .env.local and fill in. All VITE_* vars are public (baked into the build).
VITE_API_BASE=https://REPLACE.execute-api.ap-southeast-1.amazonaws.com
VITE_GOOGLE_CLIENT_ID=1080212071394-drtg41ou6bjm412teq626rf7dn8b41q6.apps.googleusercontent.com

# Set to "1" to run the UI with a mock backend (no AWS needed) — see src/lib/api.js
VITE_MOCK=1
