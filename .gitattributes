# Auto detect text files and perform LF normalization
* text=auto

# Fonts are binary — never let eol normalization touch them (a single rewritten
# byte makes the file unparseable to jsPDF and silently breaks PDF export).
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary
